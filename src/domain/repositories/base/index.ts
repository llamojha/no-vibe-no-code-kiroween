/**
 * Base repository interfaces for the domain layer
 * These interfaces define the contracts for data access operations
 */

export type { IRepository, ICommandRepository, IQueryRepository } from './IRepository';